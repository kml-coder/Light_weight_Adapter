# --- Core Deep Learning Framework ---
torch>=1.10.0 # PyTorch
# torchtext>=0.11.0 # (선택 사항: 데이터셋 로딩에 사용될 경우)
# torchvision>=0.11.0 # (선택 사항: 사용하지 않음)

# --- NLP & Transformers ---
sentencepiece>=0.1.96 # KoBERT 및 LaBSE 토크나이저 종속성

# --- Data Handling & Computation ---
numpy>=1.20.0
pandas>=1.3.0 # 데이터 정렬 결과 저장 및 분석용 (Day 1, Day 3)

# --- Alignment & Evaluation ---
scikit-learn>=1.0 # Cosine Similarity, PCA/t-SNE 등 평가 및 시각화
scipy>=1.7.0 # Hungarian Algorithm (linear_sum_assignment) 종속성

# --- Utility & Visualization ---
tqdm>=4.60.0 # 학습 진행 상황 시각화 (진행바)
matplotlib>=3.4.0 # 시각화 (PCA/t-SNE 그림)

nltk

accelerate
datasets
gluonnlp >= 0.6.0, <=0.10.0
mxnet >= 1.4.0, <=1.7.0.post2
onnxruntime
protobuf
sentencepiece >= 0.1.6, <=0.1.96
torch
transformers
openai